// After you deploy the backend to Render, replace the URL below with
// your real Render URL + "/api", e.g. "https://cryptid-watch-api.onrender.com/api"
window.API_BASE_URL = "http://localhost:5000/api";
